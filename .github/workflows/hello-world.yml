name: hello_world
on: workflow_dispatch # This means the workflow will be triggered manually or [push]
jobs:
    hello_world_job:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - name: Hello World
              run: echo Hello World